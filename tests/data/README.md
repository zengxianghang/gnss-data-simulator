# Test data

Keep checked-in fixtures small, deterministic, redistributable, and independent of live network access.

`minimal/` will contain compact RINEX/navigation fixtures introduced by the GNSS implementation issues.
